@model Movie_Catalog.Data.Models.Director

@{
    ViewData["Title"] = "Edit Director";
}

<h1>@ViewData["Title"]</h1>

<form asp-action="Edit" method="post">
    <div class="form-group">
        <label for="Name">Director Name</label>
        <input type="text" name="name" id="Name" class="form-control" value="@Model.Name" required />
    </div>
    <button type="submit" class="btn btn-primary">Save Changes</button>
</form>

<a href="@Url.Action("All")" class="btn btn-secondary">Back to List</a>